type TeamMember = {
  name: string;
  role: string;
  description: string;
  image: string;
};

const teamMembers: TeamMember[] = [
  {
    name: "Jasser",
    role: "Lead Developer",
    description: "Architecting seamless solutions.",
    image: "https://api.dicebear.com/9.x/avataaars/svg?seed=Jasser",
  },
  {
    name: "Ali",
    role: "Manager",
    description: "Visionary leadership & strategy.",
    image: "https://api.dicebear.com/9.x/avataaars/svg?seed=Ali&clothing=blazerAndShirt",
  },
  {
    name: "Khalid",
    role: "Creative Director",
    description: "Pixel-perfect design & UX.",
    image: "https://api.dicebear.com/9.x/avataaars/svg?seed=Khalid&facialHair=beardMedium",
  },
];


const TeamMember = ({ member, index }: MemberProps) => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.8, delay: index * 0.2 }}
      viewport={{ once: true }}
      className="flex flex-col items-center text-center w-64"
    >
      {/* Image (Floating Circle) */}
      <div className="mb-6 h-48 w-48 overflow-hidden rounded-full border-4 border-white shadow-lg grayscale transition-all duration-500 hover:grayscale-0 hover:scale-105">
        <img
          src={member.image}
          alt={member.name}
          className="h-full w-full object-cover"
        />
      </div>

      {/* Text Info */}
      <h3 className="text-3xl font-bold text-neutral-900">{member.name}</h3>
      <span className="mb-3 text-sm font-bold uppercase tracking-widest text-blue-600">
        {member.role}
      </span>
      <p className="text-neutral-500 leading-relaxed font-medium">
        {member.description}
      </p>
    </motion.div>
  );
};